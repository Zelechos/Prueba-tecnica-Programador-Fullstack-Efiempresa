const factorial = Numero => (Numero== 0 || Numero ==1)? 1 : Numero * factorial(Numero-1);
console.log("Hello World");
'use strict'
window.addEventListener('load', ()=> value);
const parImpar = Number => (Number % 2 == 0)? console.log(Number,' es Par') : console.log(Number,' es Impar');
let numeroAleatorio = Math.round(Math.random() * 1000);//0 al 1000
const vowels = word => (new RegExp(/[aeiou]/gi).test(word)) ? word.match(/[aeiou]/gi).length : 0;
const funcion = (value === value) ? console.log("value true") : console.log("value false");